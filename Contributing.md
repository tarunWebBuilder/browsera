Backend 
-- Start a Virtual envoirment 
`cd backend`
create a Virtual envoirment
`python venv .venv`
activate envoirment
`source .venv/bin/activate`
install libraries
`pip install -r requirements.txt`

`uvicorn main:app --reload`
--  


Frontend start Guide 
`npm install`
`npm run build`
`npm run dev`



